these files are auto generated by ftc project bolt
version is only changed when the project is updated.
robot_config includes details about the robot shared by all opModes.
formated as:
ticks per wheel rotation

and opMode(number), which contains metadata and node (control) data for each opMode. this is formatted as:
auto/teleOp
name
group
auto transition to
node 1 type:
